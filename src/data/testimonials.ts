import type { Testimonial } from "../types/testimonial";

/**
 * Depoimentos de colegas, líderes ou clientes.
 * Adicione novas entradas seguindo o shape do tipo Testimonial.
 * O componente `Testimonials` renderiza automaticamente quando esta lista tem itens.
 */
export const testimonials: Testimonial[] = [];
